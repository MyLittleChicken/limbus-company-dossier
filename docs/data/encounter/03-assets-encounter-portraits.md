# 회차 3 — `data/assets/encounters` 적 초상

> **애셋** · `limbus-assets` · **506개** · 10 MB · 전부 `{n}_portrait.webp`
> 출처 커밋 `774883d7` · 스냅샷 2026-07-25 · 정리 2026-07-31
> **인카운터 편(회차 1–3)의 마지막 회차 · 마스터북 마지막 회차**

---

## 1. 명명 규칙 — 접미가 하나뿐이다

```
{portrait}_portrait.webp        506개 · 접미 100 % 동일
```

인격(4종)·E.G.O(3종)와 달리 **변형이 없다.** 적은 초상 하나씩이다.

## 2. **마스터북 유일의 애셋 결손 2건**

```
portrait 유일 458   →  애셋 있는 것 456 · 없는 것 2
                       1286 · 1307
```

| 엔티티 | 애셋 | 결손 |
| --- | ---: | ---: |
| 인격 | 712 | 0 |
| E.G.O | 318 | 0 |
| 기프트 | 476 | 0 |
| 팩 | 155 | 0 |
| 상태 | 1,193 | 0 |
| **인카운터** | **506** | **2** |

여섯 엔티티 3,360개 중 **결손이 이 둘뿐**이다.

## 3. 애셋에만 있는 50개 — 절반이 로케일에 있다

```
애셋 506 − portrait 456 = 50
그중 로케일 `Enemies*` 에 이름이 있는 것   25
```

**25종은 이름이 있는데 어느 인카운터에도 안 나온다.** 삭제된 전투이거나
아직 배치되지 않은 적으로 보인다. 나머지 25종은 이름조차 없다.

---

## 함정 요약

1. **`1286` · `1307` 초상이 없다.** 마스터북 전체에서 유일한 애셋 결손
2. 애셋에만 있는 50개 중 **25개는 로케일에 이름이 있다**
3. 접미 `_portrait` 를 붙여야 조회된다

## 미해결

없다. 506개 전부 확정했다.

## 근거 재현

```
data/assets/encounters/limbus-assets/            506개 · _portrait
data/entities/encounters/limbus-assets/*.json    portrait 458
data/entities/encounters/loc-ko/*.json           이름 1,342종
```
